# Web scraping dependencies (existing)
selenium
webdriver-manager
requests
pdfplumber
langdetect
urllib3
beautifulsoup4  # HTML parsing helpers
tqdm

# FastAPI and web framework
fastapi
uvicorn
python-multipart

# Machine Learning and AI (Updated for Python 3.12 compatibility)
transformers
torch
torchvision
scikit-learn
numpy
pandas
aiofiles

# Natural Language Processing
spacy
nltk

# Database
sqlalchemy
# Note: sqlite3 is built into Python, no separate install needed

# Streamlit for frontend
streamlit
plotly
altair

# Document processing
PyPDF2
python-docx

# Build tools (essential for Python 3.12)
setuptools
wheel

# Utilities
python-dotenv
pydantic
pydantic-settings
loguru
pytest
pytest-asyncio  # Async test support (Phase 8)
httpx>=0.25,<0.28  # Pinned: httpx 0.28 removed Client(app=) which breaks starlette TestClient
aiohttp  # Async HTTP client for Ollama
psutil  # System resource monitoring for health endpoint
apscheduler  # Scheduled scraping (Phase 1)
redis  # Cache backend when REDIS_URL is set (Phase 5)

# Database
supabase

# Vector Database for RAG
chromadb
faiss-cpu

# LLM API clients
groq  # Primary LLM provider (80% faster than local)
ollama  # Optional fallback for local inference
